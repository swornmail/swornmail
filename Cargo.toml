[package]
name = "swornmail"
version = "0.0.1"
edition = "2021"
description = "Reference constants and types for the SwornMail protocol (cryptographic IPv6 prefix attestation for email senders). Early development — protocol draft in progress."
license = "Apache-2.0"
repository = "https://github.com/swornmail/swornmail"
homepage = "https://swornmail.dev"
keywords = ["email", "smtp", "ipv6", "reputation", "attestation"]
categories = ["email"]

[dependencies]
